<?php
/**
 * class ClubController
 * @brief handles controller for posts
 */
class ClubController extends Controller {
  /**
   * Loads appropriate model and request to database
   * for rendering club page.
   */
  function index() {
  }
}
